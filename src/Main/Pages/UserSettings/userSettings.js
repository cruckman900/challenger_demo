import React from "react";
import classes from './userSettings.module.css';

export default function userSettings() {
    return (
        <div className={classes.userSettings}>User Setup</div>
    );
}